# Web Helper

`index.html` is a self-contained static web application. It embeds the current
collection PowerShell script as base64, so the generated CMD can run without
downloading code from GitHub.

Open `index.html` in a browser or host it with any static web server.
